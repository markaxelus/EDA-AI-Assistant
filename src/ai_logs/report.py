from pathlib import Path
from typing import Dict, List
from .schema import Cluster, Summary

def make_markdwon(clusters: List[Cluster], summaries: Dict[str, Summary], feedback_url: str) -> str:
  return

def generate_summary_stats(clusters: List[Cluster], summaries: Dict[str, Summary])
